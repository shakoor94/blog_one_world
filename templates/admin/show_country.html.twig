{% extends 'base.html.twig' %}

{% block body %}
  <h1>Liste des pays</h1>

  <ul>
    {% for countrie in countries %}
      <li>
        <a href="{{ path('show_country', { 'id': country.getId() }) }}">{{ country.getTitle() }}</a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
