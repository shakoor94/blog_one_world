{# {% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des pays</h1>
    <ul>
        {% for country in countries %}
            <li><a href="{{ path('show_country', { 'title': country.title }) }}">{{ country.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %} #}
